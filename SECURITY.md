# 보안 요구사항

브라우저에서 계산한 점수는 신뢰할 수 없습니다. `VITE_API_BASE_URL`을 설정해 실제
점수 API를 연결하기 전에 서버에서 다음 규칙을 모두 강제해야 합니다.

- 닉네임을 NFKC 방식으로 정규화한 후 검증합니다. 한글, 영문, 숫자, 공백, `_`, `-`만
  허용하고 길이는 최대 10자로 제한합니다.
- 모든 숫자 필드가 허용 범위 안의 음수가 아닌 정수인지 검증합니다.
- 점수, 스테이지, 콤보, 정답 수, 시간정지 사용 횟수, 플레이 시간의 조합이 실제 게임
  규칙상 가능한지 검증하고 불가능한 기록은 거부합니다.
- 게임 시작 전에 유효기간이 짧은 게임 세션 ID를 발급하고, 완료된 세션은 한 번만
  제출할 수 있게 합니다. 해당 세션에서 얻을 수 있는 최대 점수도 서버에서 다시
  계산합니다.
- 점수 등록과 랭킹 조회에 IP 및 게임 세션 기준 요청 횟수 제한을 적용합니다.
- 익명 사용자의 자동 점수 등록이 문제가 되면 CAPTCHA 등의 봇 방지 기능을 추가합니다.
- 닉네임은 사용자 신원이 아니라 화면에 표시하는 이름으로만 취급하고, 부적절한
  닉네임과 기록을 운영자가 차단하거나 삭제할 수 있게 합니다.
- 쿠키 기반 로그인을 추가한다면 쿠키에 `Secure`, `HttpOnly`, `SameSite` 속성을
  설정하고, 데이터를 변경하는 요청에는 CSRF 토큰을 요구합니다.
- CORS는 실제로 배포된 게임 주소만 허용해야 하며, 요청에서 받은 출처를 검사 없이
  그대로 허용해서는 안 됩니다.

`public/_headers`에는 Cloudflare Pages와 Netlify가 인식하는 운영용 보안 헤더가
정의되어 있습니다. 다른 호스팅 서비스를 사용한다면 해당 서비스의 배포 설정에
동일한 헤더를 직접 등록해야 합니다. HSTS는 반드시 HTTPS 환경에서만 제공해야 합니다.
