# 데이터 정제 유지보수 스크립트

북한말 원천 데이터를 검증·정제할 때 사용했던 일회성 도구 모음이다. 앱 빌드나
운영 배포에서는 실행되지 않는다. 각 스크립트는 입력 JSON을 현재 작업 디렉터리에서
찾으므로, 다시 사용할 때는 필요한 원천 파일을 별도 작업 디렉터리에 준비하고
스크립트의 입출력 경로를 먼저 확인한다.

운영 게임 데이터는 public/data/chosung_words.json과
public/data/acidrain_pairs.json 두 파일만 사용한다. 과거 백업은 Git 이력에서
복구한다.