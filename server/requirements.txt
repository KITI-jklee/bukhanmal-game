fastapi==0.141.1
uvicorn[standard]==0.52.1
sqlalchemy==2.0.51
pydantic==2.13.4
python-dotenv==1.2.2
psycopg[binary]==3.2.10
# starlette(fastapi의 전이 의존성)가 아직 anyio.abc.BlockingPortal(구 경로)을
# 참조하는데, anyio 4.15.0부터 이 경로가 deprecated alias로 바뀌면서
# `pytest -W error`에서 TestClient를 import하자마자 DeprecationWarning이
# 에러로 승격되어 테스트가 전부 실패한다. starlette가 anyio.from_thread의
# 새 경로로 옮겨갈 때까지 마지막 정상 버전으로 고정.
anyio==4.14.2